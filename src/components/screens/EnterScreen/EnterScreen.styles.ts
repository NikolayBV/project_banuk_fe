import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  EnterScreenWrapper: {
    width: '100%',
    height: '100%',
    display: 'flex',
    alignItems: 'center',
  },
  EnterHeading: {
    fontSize: 18,
    color: '#000',
  },
});
